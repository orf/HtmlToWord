# ToDo: Fill this with useful groups (Format tags, Text tags, etc) if needed
from HtmlToWord.elements.Text import Bold, Italic, UnderLine

# ToDo: finish these groups

FORMAT_TAGS = (
    Bold.GetName(), Italic.GetName(), UnderLine.GetName()
)